{
 "name": "ln-church-monorepo",
 "private": true,
 "workspaces": [
  "packages/*",
  "packages/verifiers/*",
  "examples/*"
 ],
 "scripts": {
  "build": "npm run build --workspaces --if-present",
  "test": "vitest run",
  "test:watch": "vitest"
 },
 "devDependencies": {
  "@types/node": "^25.6.0",
  "@vitest/ui": "^4.1.4",
  "typescript": "^6.0.2",
  "vitest": "^4.1.4"
 }
}
