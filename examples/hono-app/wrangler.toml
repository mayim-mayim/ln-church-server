name = "ln-church-monzenmachi-api"
compatibility_date = "2024-09-23"
compatibility_flags = [ "nodejs_compat" ]
main = "src/index.ts"

# 本番環境へのデプロイ時は、以下のコマンドでシークレットを安全に登録します：
# npx wrangler secret put MACAROON_SECRET

[[kv_namespaces]]
binding = "RECEIPT_KV"
id = "xxxxxxxxxxxxxxxxx" # 本番デプロイ時に実際のIDに置き換えます
preview_id = "mock-local-kv-id-12345" # ローカル開発(npm run dev)用のお手軽モックID